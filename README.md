## Movie Rental App

run this command on terminal

```
cd ./movie-rental-backend
npm install
```

run `mongod` command to start mongo db daemon services and then `mongo` to run mongo shell.
open another terminal window and run `nodemon index` command to start backend services.

use commands below to start front-end application.

```
cd ../movie-rental-front
yarn install
yarn start
```
